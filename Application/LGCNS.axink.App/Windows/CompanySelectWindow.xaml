<Window x:Class="LGCNS.axink.App.Windows.CompanySelectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LGCNS.axink.App.Windows"
        mc:Ignorable="d"
        Title="{DynamicResource Dic_CompanySelect}" Width="420" Height="340"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    <Window.Resources>
        <!-- 드롭다운 토글 버튼 -->
        <ControlTemplate x:Key="ComboBoxToggle" TargetType="ToggleButton">
            <Border x:Name="Bd" Background="White" BorderBrush="#CCC"
                BorderThickness="1" CornerRadius="8" Padding="12,0">
                <Grid>
                    <ContentPresenter/>
                    <Path x:Name="Arrow" Data="M0,0 L4,4 L8,0" Stroke="#888"
                      StrokeThickness="1.5" Fill="Transparent"
                      HorizontalAlignment="Right" VerticalAlignment="Center"
                      RenderTransformOrigin="0.5,0.5">
                        <Path.RenderTransform>
                            <RotateTransform Angle="0"/>
                        </Path.RenderTransform>
                    </Path>
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="Bd" Property="BorderBrush" Value="#999"/>
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="Bd" Property="BorderBrush" Value="#185FA5"/>
                    <Setter TargetName="Arrow" Property="RenderTransform">
                        <Setter.Value>
                            <RotateTransform Angle="180"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <!-- ComboBox 본체 -->
        <Style x:Key="StyledComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                      Template="{StaticResource ComboBoxToggle}"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay,
                                                  RelativeSource={RelativeSource TemplatedParent}}"
                                      Focusable="False" ClickMode="Press"/>

                            <ContentPresenter x:Name="ContentSite"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          Margin="14,0,28,0" VerticalAlignment="Center"
                                          HorizontalAlignment="Left" IsHitTestVisible="False"/>

                            <!-- 플레이스홀더 -->
                            <TextBlock x:Name="Placeholder" Text="회사를 선택하세요"
                                   Foreground="#AAA" FontSize="14"
                                   Margin="14,0,28,0" VerticalAlignment="Center"
                                   IsHitTestVisible="False"
                                   Visibility="Collapsed"/>

                            <!-- 드롭다운 팝업 -->
                            <Popup x:Name="PART_Popup" Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True" Focusable="False"
                               PopupAnimation="Slide">
                                <Border x:Name="DropDown" MinWidth="{TemplateBinding ActualWidth}"
                                    MaxHeight="200" Margin="0,4,0,0"
                                    Background="White" BorderBrush="#E0E0E0"
                                    BorderThickness="1" CornerRadius="8"
                                    Padding="4">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#000" Opacity="0.08"
                                                      BlurRadius="12" ShadowDepth="4"
                                                      Direction="270"/>
                                    </Border.Effect>
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <StackPanel IsItemsHost="True"
                                                KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="SelectedItem" Value="{x:Null}">
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 드롭다운 항목 -->
        <Style x:Key="StyledComboBoxItem" TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Padding="{TemplateBinding Padding}"
                            Background="Transparent" CornerRadius="6">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#F5F7FA"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#E6F1FB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border Background="White" CornerRadius="12"
            BorderBrush="#E0E0E0" BorderThickness="1">
        <Grid Margin="32,28,32,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- 상단 헤더 -->
                <RowDefinition Height="*"/>
                <!-- 중앙 콘텐츠 (추후) -->
                <RowDefinition Height="Auto"/>
                <!-- 하단 버튼 -->
            </Grid.RowDefinitions>

            <!-- 상단: 아이콘 + 타이틀 + 설명 -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
                <Border Width="48" Height="48" CornerRadius="24"
                        Background="#E6F1FB" HorizontalAlignment="Center" Margin="0,0,0,12">
                    <Path Data="M3,9 L12,2 L21,9 V20 A2,2 0 0 1 19,22 H5 A2,2 0 0 1 3,20 Z"
                          Stroke="#185FA5" StrokeThickness="1.8" Fill="Transparent"
                          Width="22" Height="22" Stretch="Uniform"
                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="{DynamicResource Dic_CompanySelect}" FontSize="17" FontWeight="SemiBold"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="{DynamicResource Msg_SelectCompany}" FontSize="13" Foreground="#888"
                           HorizontalAlignment="Center" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- 중앙: 회사 선택 -->
            <StackPanel Grid.Row="1">
                <ComboBox x:Name="CmbCompanies"
                  Style="{StaticResource StyledComboBox}"
                  ItemContainerStyle="{StaticResource StyledComboBoxItem}"
                  SelectionChanged="CmbCompanies_SelectionChanged">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding CompanyName}" FontSize="14"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                <TextBlock x:Name="TxtError" Foreground="#E24B4A" FontSize="12"
               Margin="0,6,0,0" TextWrapping="Wrap"/>
            </StackPanel>

            <!-- 하단: 버튼 -->
            <Grid Grid.Row="2" Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="{DynamicResource Dic_Common_Cancel}" Height="38" FontSize="14"
                        Background="Transparent" BorderBrush="#CCC" BorderThickness="1"
                        Cursor="Hand" Click="BtnCancel_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd" Background="Transparent"
                                    BorderBrush="#CCC" BorderThickness="1" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#F5F5F5"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button Grid.Column="2" Content="{DynamicResource Dic_Common_Ok}" Height="38" FontSize="14"
                        Foreground="White" Cursor="Hand" Click="BtnOk_Click" IsDefault="True">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd" Background="#185FA5"
                                    CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#1565B8"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#0C447C"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>

        </Grid>
    </Border>
</Window>

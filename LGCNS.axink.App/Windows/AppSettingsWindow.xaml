<Window x:Class="LGCNS.axink.App.Windows.AppSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LGCNS.axink.App.Windows"
        mc:Ignorable="d"
        Title="AppSettingsWindow" Height="170" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Web 페이지 주소" FontWeight="SemiBold"/>

        <TextBox Grid.Row="1" Margin="0,10,0,0" Height="30" VerticalContentAlignment="Center"
                 Text="{Binding WebViewSource, UpdateSourceTrigger=PropertyChanged}"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Width="100" Margin="0,0,8,0" Content="취소" Command="{Binding CancelCommand}"/>
            <Button Width="100" Content="저장" IsDefault="True" Command="{Binding SaveCommand}"/>
        </StackPanel>
    </Grid>
</Window>